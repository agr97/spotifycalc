// reudcer takes 2 things
// action
// copy of current state
// then returns the store

function playlist(state = {}, action) {
  console.log(state, action);
  return state;
}


export default playlist;
